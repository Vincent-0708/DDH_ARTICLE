import { Address } from "../../client/domain/value-objects/address.value-object";
import { PizzaEntity } from "../domain/create_pizza/entities/pizza.entity";

export const paulUnderstandsDeliveryAddress = ( _deliveryAddress: Address ) : boolean => {
    //No business logic here only CRUD operations or call to APIs 
    return ( true );
};

export const paulEntersDeliveryAddressInHisGps = async () : Promise<void> => {
    //No business logic here only CRUD operations or call to APIs 
};

export const paulPutThePizzasIntoHisTopCase = ( _pizzas: PizzaEntity[] ) : void =>{
    //No business logic here only CRUD operations or call to APIs 

};

export const paulMovesWithHisScooterToDeliveryAddress = async () : Promise<void> => {
    //No business logic here only CRUD operations or call to APIs 
};